on:
  - workflow_dispatch
jobs:
  release:
    permissions:
      id-token: write
      attestations: write
    runs-on: ubuntu-latest
    steps:
      - name: Download Artifacts
        id: dowload
        uses: actions/download-artifact@v4
